from django.apps import AppConfig  

class InformesConfig(AppConfig):  
    default_auto_field = 'django.db.models.BigAutoField'  
    name = 'informes'  # Asegúrate de que esto coincida con el nombre de la carpeta